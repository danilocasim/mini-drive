const addDriveDialog = document.getElementById("addDriveDialog");
const addDriveBtn = document.getElementById("addDriveBtn");

addDriveBtn.addEventListener("click", () => {
  addDriveDialog.showModal();
});
