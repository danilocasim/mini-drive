<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Details</title>
    <link rel="stylesheet" href="../../style.css" />
  </head>
  <body>
    <%- include("../components/nav.ejs") %> <% if(locals.data) { %>

    <div class="container">
      <a class="link" href="/folder/<%= folderId  %>">Go Back</a>
      <p>Name: <%= data.name %></p>
      <p>Mimetype: <%= data.metadata.mimetype %></p>
      <p>Last Modified: <%= data.metadata.lastModified %></p>
      <p>Size: <%= data.metadata.size %></p>
    </div>

    <% } %>
  </body>
</html>
